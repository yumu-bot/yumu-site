image: node:10.22.0
pages:
  stage: deploy
  script:
    - yarn install # npm install
    - yarn docs:build # npm run docs:build
    - mkdir .public
    - cp -r ./* .public
    - rm -rf public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master